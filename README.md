# Live-Chat-App Using Flask(python)
Uses Flask Sockets to create a live chat room application.

To run it clone the repo and run the main.py file using python3 interpreter which will redirect to port 5000
